
#include <iostream>
#include <string>
using namespace std;
void pageReplacement(int freeFrames, int refrenceString[], string algorithm) {
    int pageFaults = 0;
    int refrenceStringPtr = 0;
    int frames[freeFrames];
    int framesPtr = 0;
    int refrenceStringLength = sizeof(refrenceString) / sizeof(refrenceString[0]);
    if (algorithm == "FIFO") {
        for (int i = 0; i < freeFrames; ++i) {
            frames[i] = -1;
        }
        for (int j = refrenceStringPtr; j < refrenceStringLength; ++j) {
            if (framesPtr > freeFrames - 1) {
                framesPtr = 0;
            }
            //find if the element is availble or not
            for (int i = 0; i < freeFrames; ++i) {
                if (frames[i] == refrenceString[refrenceStringPtr]) {
                    continue;
                }
                else {
                    frames[framesPtr] = refrenceString[refrenceStringPtr];
                    ++framesPtr;
                    ++pageFaults;
                }
            }
        }
    }
}
int main() {

    int frameNumber = 5;
    int refrenceString[7]={7, 0, 1, 2, 0, 5, 1};

        string algo = "FIFO";

    pageReplacement(frameNumber, refrenceString, algo);

    return 0;
}
